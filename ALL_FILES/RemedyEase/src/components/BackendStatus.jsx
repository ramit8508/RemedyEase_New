import React from 'react';

const BackendStatus = () => {
  // This component is disabled for local development
  // Re-enable when deploying to production
  return null;
};

export default BackendStatus;

